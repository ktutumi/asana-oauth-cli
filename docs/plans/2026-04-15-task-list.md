# Tasks List Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Asana project 配下の task 一覧を取得する `tasks list` CLI コマンドを追加する。

**Architecture:** 既存の `projects list` と同じ責務分離を保ち、Asana API 呼び出しは `src/asana-api.ts`、CLI 配線は `src/cli.ts`、利用者向け説明は `README.md` と OAuth scope 定義の `src/oauth.ts` に反映する。`tasks list` は最小機能として `--project <gid>` を必須にし、Asana の project tasks endpoint をページネーション付きで読む。

**Tech Stack:** Node.js 20+, TypeScript, Commander, Vitest, pnpm

---

### Task 1: Add failing CLI test for `tasks list`

**Objective:** `tasks list --project <gid>` が保存済み token を使って task 一覧を表示する期待値を固定する。

**Files:**
- Modify: `tests/cli.test.ts`
- Test: `tests/cli.test.ts`

**Step 1: Write failing test**

```ts
it('lists tasks for a project with tasks list', async () => {
  const stdout: string[] = [];
  const configDir = mkdtempSync(join(tmpdir(), 'asana-oauth-cli-cli-'));
  const configPath = join(configDir, 'credentials.json');

  writeFileSync(configPath, JSON.stringify({
    clientId: 'client-1',
    redirectUri: 'http://127.0.0.1:18787/callback',
    token: {
      access_token: 'access-1',
      refresh_token: 'refresh-1',
      token_type: 'bearer',
    },
  }));

  vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
    ok: true,
    json: async () => ({ data: [{ gid: '101', name: 'Ship pnpm migration' }], next_page: null }),
  }));

  await runCli(['--config', configPath, 'tasks', 'list', '--project', 'project-1'], {
    stdout: (line) => stdout.push(line),
    stderr: vi.fn(),
    exit: (code) => {
      throw new Error(`unexpected exit ${code}`);
    },
  });

  expect(stdout[0]).toContain('Ship pnpm migration');
});
```

**Step 2: Run test to verify failure**

Run: `pnpm test -- --run tests/cli.test.ts`
Expected: FAIL — unknown command `tasks` or missing implementation.

**Step 3: Write minimal implementation**

この段階ではまだ実装しない。RED を確認してから次タスクへ進む。

**Step 4: Run test to verify pass**

このタスクでは未実施。

**Step 5: Commit**

```bash
git add tests/cli.test.ts
git commit -m "test: cover tasks list command"
```

### Task 2: Add failing API test for project task listing

**Objective:** Asana task list endpoint 呼び出しとページネーションを `src/asana-api.ts` に要求する。

**Files:**
- Modify: `tests/asana-api.test.ts`
- Test: `tests/asana-api.test.ts`

**Step 1: Write failing test**

```ts
it('lists tasks for a project across paginated responses', async () => {
  const fetchMock = vi
    .fn()
    .mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        data: [{ gid: '101', name: 'Ship pnpm migration' }],
        next_page: { offset: 'next-1' },
      }),
    })
    .mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        data: [{ gid: '102', name: 'Review CI' }],
        next_page: null,
      }),
    });
  vi.stubGlobal('fetch', fetchMock);

  const tasks = await listTasks('access-1', 'project-1');

  expect(fetchMock).toHaveBeenNthCalledWith(
    1,
    'https://app.asana.com/api/1.0/projects/project-1/tasks',
    expect.objectContaining({
      headers: {
        accept: 'application/json',
        authorization: 'Bearer access-1',
      },
    }),
  );
  expect(fetchMock).toHaveBeenNthCalledWith(
    2,
    'https://app.asana.com/api/1.0/projects/project-1/tasks?offset=next-1',
    expect.objectContaining({
      headers: {
        accept: 'application/json',
        authorization: 'Bearer access-1',
      },
    }),
  );
  expect(tasks).toEqual([
    { gid: '101', name: 'Ship pnpm migration' },
    { gid: '102', name: 'Review CI' },
  ]);
});
```

**Step 2: Run test to verify failure**

Run: `pnpm test -- --run tests/asana-api.test.ts`
Expected: FAIL — `listTasks` is not exported yet.

**Step 3: Write minimal implementation**

`src/asana-api.ts` に `listTasks(accessToken, project)` を追加し、`projects/{gid}/tasks` をページネーション付きで読む。

**Step 4: Run test to verify pass**

Run: `pnpm test -- --run tests/asana-api.test.ts`
Expected: PASS

**Step 5: Commit**

```bash
git add tests/asana-api.test.ts src/asana-api.ts
git commit -m "feat: add Asana task listing API"
```

### Task 3: Wire the CLI command

**Objective:** `runCli` から `tasks list --project <gid>` を使えるようにする。

**Files:**
- Modify: `src/cli.ts`
- Test: `tests/cli.test.ts`

**Step 1: Write failing test**

Task 1 の RED を利用済み。追加の失敗テストは不要。

**Step 2: Run test to verify failure**

Run: `pnpm test -- --run tests/cli.test.ts`
Expected: FAIL — `tasks list` 未実装。

**Step 3: Write minimal implementation**

```ts
const tasks = program.command('tasks').description('task operations');
tasks
  .command('list')
  .description('list tasks in a project')
  .requiredOption('--project <gid>', 'project gid')
  .action(async (options: { project: string }) => {
    const accessToken = await requireAccessToken(program.opts().config as string);
    const items = await listTasks(accessToken, options.project);
    io.stdout(JSON.stringify(items, null, 2));
  });
```

**Step 4: Run test to verify pass**

Run: `pnpm test -- --run tests/cli.test.ts`
Expected: PASS

**Step 5: Commit**

```bash
git add src/cli.ts tests/cli.test.ts
git commit -m "feat: add tasks list command"
```

### Task 4: Update OAuth defaults and documentation

**Objective:** `tasks list` が使えるよう既定 scope と README の説明を揃える。

**Files:**
- Modify: `src/oauth.ts`
- Modify: `tests/oauth.test.ts`
- Modify: `README.md`

**Step 1: Write failing test**

```ts
it('exposes conservative explicit default scopes for current commands', () => {
  expect(defaultScopes).toEqual(['users:read', 'workspaces:read', 'projects:read', 'tasks:read']);
});
```

**Step 2: Run test to verify failure**

Run: `pnpm test -- --run tests/oauth.test.ts`
Expected: FAIL — `tasks:read` がまだ含まれていない。

**Step 3: Write minimal implementation**

`src/oauth.ts` の `defaultScopes` に `tasks:read` を追加し、README の対応コマンド・既定 scope・使用例へ `tasks list --project ...` を追記する。

**Step 4: Run test to verify pass**

Run: `pnpm test -- --run tests/oauth.test.ts`
Expected: PASS

**Step 5: Commit**

```bash
git add src/oauth.ts tests/oauth.test.ts README.md
git commit -m "docs: document tasks list scope and usage"
```

### Task 5: Full verification

**Objective:** 変更全体が既存機能を壊していないことを確認する。

**Files:**
- Modify: なし
- Test: `tests/*.test.ts`

**Step 1: Run focused tests**

Run: `pnpm test -- --run tests/cli.test.ts tests/asana-api.test.ts tests/oauth.test.ts`
Expected: PASS

**Step 2: Run full suite**

Run: `pnpm test`
Expected: PASS

**Step 3: Build and typecheck**

Run: `pnpm build && pnpm lint:types`
Expected: PASS

**Step 4: Independent review**

Run the requesting-code-review flow on the final diff.
Expected: PASS

**Step 5: Commit**

```bash
git add -A
git commit -m "[verified] feat: add tasks list command"
```
